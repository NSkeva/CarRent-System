@model IReadOnlyList<Partner>
@foreach (var partner in Model)
{
    <tr>
        <td>@partner.CompanyName</td>
        <td>@partner.ContactPerson</td>
        <td>@partner.Phone</td>
        <td>@partner.Email</td>
        <td class="row-actions">
            <a asp-action="Edit" asp-route-id="@partner.Id" class="btn btn-ghost btn-sm">Uredi</a>
            <form asp-action="Delete" asp-route-id="@partner.Id" method="post" class="inline-form">
                @Html.AntiForgeryToken()
                <button type="submit" class="btn btn-ghost btn-sm" style="color:var(--danger)">Obriši</button>
            </form>
        </td>
    </tr>
}
