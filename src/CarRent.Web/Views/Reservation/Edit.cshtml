@model ReservationFormVm
@{ ViewData["Title"] = "Uredi rezervaciju"; ViewData["Breadcrumbs"] = BreadcrumbHelper.Build("Home", "Rezervacije", $"#{Model.Id}"); }
<h1 style="font-size:22px;font-weight:800;margin-bottom:var(--space-md)">Uredi rezervaciju #@Model.Id</h1>
<partial name="_FormPartial" />
@section Scripts { <partial name="_ValidationScriptsPartial" /> }
