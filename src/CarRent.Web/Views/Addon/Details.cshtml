@model Addon

<section class="panel detail-card">
    <h1>@Model.Name</h1>
    <p><strong>Cijena po danu:</strong> @Model.PricePerDay EUR</p>
    <div class="panel-actions">
        <a asp-action="Index" class="btn btn-secondary btn-sm">← Natrag na listu</a>
    </div>
</section>
